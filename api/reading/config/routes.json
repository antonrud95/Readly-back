{
  "routes": [
    {
      "method": "GET",
      "path": "/readings",
      "handler": "reading.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/readings/count",
      "handler": "reading.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/readings/:id",
      "handler": "reading.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/readings",
      "handler": "reading.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/readings/:id",
      "handler": "reading.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/readings/:id",
      "handler": "reading.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
